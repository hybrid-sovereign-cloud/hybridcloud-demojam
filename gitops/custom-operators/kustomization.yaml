apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# CRDs live in wave-38 app hs-crds (./crds). This app is Deployments + image-wait only.
resources:
  - entity-operator.yaml
  - platform-operators.yaml
  - tenant-operators.yaml
  - image-wait.yaml
