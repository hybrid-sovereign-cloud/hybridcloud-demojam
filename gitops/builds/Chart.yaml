# OPTIONAL air-gap only — not used in default ZTP.
# Default: provision.builds=false; Deployments pull quay.io/gauravshankar/*.
# Enable hs-builds only when the hub cannot reach Quay and must build locally.
apiVersion: v2
name: hs-builds
description: Optional OpenShift BuildConfigs/ImageStreams for air-gap (disabled by default)
type: application
version: 0.1.13
appVersion: "0.1.0"
